# MongoDB Connection String
# Local MongoDB
MONGODB_URI=mongodb://localhost:27017/perseverance

# Or use MongoDB Atlas (replace with your credentials)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/perseverance?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret
JWT_SECRET=your_jwt_secret_key

# CORS Origin
CORS_ORIGIN=http://localhost:5173
